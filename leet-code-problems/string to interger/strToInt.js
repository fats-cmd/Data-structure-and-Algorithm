/**
 * @param {string} s
 * @return {number}
 */
var myAtoi = function(s) {
    
    // convert the string to an array of characters
    let convertStr = s.split('');
    // if()
    // switch(convertStr){
    //     case startsWith():
    // }
  
    return convertStr
};

let result = myAtoi("337c0d3")
console.log( result)